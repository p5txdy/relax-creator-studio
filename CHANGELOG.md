# 更新记录

## v1.1

- 更新 Windows 与 macOS 运行器：Windows 普通入口统一打开正式 EXE，缺少安装包时自动准备源码环境；macOS 新增 `启动漫画推文-Mac.command` 普通入口，两个平台的开发启动器均支持首次运行自动创建独立环境和安装依赖。GitHub Actions 继续同时构建 Windows x64、Apple Silicon DMG 与 Intel DMG。
- 新增“人物场景项管理”和“删除项”：主页顶部及概览区都提供醒目的删除入口，管理窗口可查看每个项的人物、场景与关联任务数量后删除，工作台可直接“删除当前项”；删除会永久清理项内候选图和固定参考图。若仍有关联任务，任务会自动转入新的空白项，保留小说与分镜原文，并清除已失效的人物/场景绑定。同时修复启动脚本一直优先打开旧版 EXE、导致新按钮不显示的问题。
- 修复新建项目默认关联第一个已有项、因而看到其他项目参考图的问题；现在默认建立空白人物场景项，只有用户主动选择已有项时才共享人物、场景和图片，并在弹窗中明确显示当前行为。
- 修复人物场景项命名入口不明显的问题：新建项目弹窗始终显示“新项名称”输入框，工作台当前关联项旁新增“重命名”按钮，修改后所有关联任务同步显示新名称。
- 人物与场景共享方式升级为可新建、可关联的“人物场景项”：新建推文时可选择已有项或创建新项，任务自动带出项内资产，后续新增人物/场景也自动同步回项；同项任务共享、不同项完全隔离，旧版资产无损迁移到默认项。
- 场景库升级为跨项目共享资产：新建或切换项目可直接调用所有已确认场景，场景候选图与固定参考图统一保存到公共本地目录；旧项目场景会自动迁移，场景改名/删除同步更新全部项目分镜，并继续支持整库导入导出。
- 修复高 DPI/系统显示缩放下“新建项目”弹窗底部确认按钮被裁掉的问题；操作栏固定在底部，弹窗按内容和屏幕居中，并支持回车确认、Esc 取消。
- 新增 GitHub Actions 双平台构建：自动产出 Windows x64 EXE、Apple Silicon DMG 与 Intel DMG 下载包。
- 补齐 Windows 与 macOS 的源码运行和打包说明；Windows 依赖增加 Pillow，开发启动脚本修正为查找 v1.1，并优先使用独立虚拟环境；macOS 明确支持在 Apple 芯片或 Intel Mac 本机生成对应架构的 `.app` 与 DMG。
- 新增项目封面制作功能：封面标题、提示词、人物/场景参考和生成结果随项目持久化，可在批量出图页编辑、预览和重新生成。
- 修复封面窗口主要人物参考无法展开选择的问题；人物和场景改为始终可见的单击选择列表，无需下拉或按 Ctrl。
- 封面改为每次固定生成四张：3:4 两张、4:3 两张，并使用两套中近景构图；标题强制放在画面水平居中、垂直中间偏下的位置，四张结果在封面窗口一一对应展示并支持单张放大。
- 优化大项目运行速度：批量出图改为仅渐进加载屏幕附近的缩略图，静态分镜分页减至每页 12 条；缓存圆角和缩略图，滚动条改为轻量绘制，连续行内编辑合并写盘，项目备份最多每 30 秒生成一次。
- 修复“AI 小说改文”页面改写模式、目标风格、叙事视角和目标篇幅下拉框无法展开的问题；同时修复其他页面同类下拉框的多词字体解析错误。
- 在“AI 小说改文”页面顶部增加始终可见的“查看提示词”按钮，可直接预览并复制当前章节实际发送给 AI 的系统提示词和用户提示词；左侧原入口继续保留。
- 小说改文新增并默认启用“短视频悬念解说（推荐）”：生成可直接配音的解说稿，前两句抛冲突、每 2—4 句持续增加信息或升级危机、压缩无效铺垫与大段对白、结尾保留真实剧情钩子，并明确禁止捏造反转或提前泄露后文。
- 修复“查看提示词”按钮在尚未导入章节时无法打开预览的问题：现在无论有当前章节、只有粘贴正文还是完全空白，都会打开独立提示词窗口；空白项目显示完整解说提示词模板，已导入项目显示实际发送内容，并可一键复制。
- 修复 Seedream 5.0 Pro 选择 1K 时的尺寸配置：按照接口最低 3,686,400 像素要求转换画幅（例如 9:16 使用 1440×2560），并在请求前拦截像素不足的自定义尺寸。
- 修复批量出图页输出分辨率下拉框无法展开或选择不生效的问题，改为常驻分辨率按钮并在点击后立即保存。
- 分辨率选项现在跟随模型：Seedream 5.0 Lite 只显示 2K/3K，Pro 显示 1K/2K/3K/4K；切换到 Lite 时会自动纠正不兼容的 1K/4K 设置。
- 汇总发布 Seedream 5.0 Lite / 高质量模型切换、官方模型 ID 与分辨率参数修复。
- 静态分镜全面采用中近景、近景构图，并加强角色、服装与固定场景参考的一致性。
- 批量出图支持无需 Ctrl 的镜头多选，以及只重新绘制选中镜头的工作流。
- 完善项目、角色和场景的本地持久化、共享、导入导出及剪映静态漫草稿流程。
- 新增 Apple Silicon 与 Intel 两套 macOS v1.1 DMG 自动构建；修复旧脚本版本号、Homebrew 依赖名及 `pyJianYingDraft` 资源路径错误。
- Mac 打包自检会真实解析 WAV 并写出剪映草稿 JSON，避免发布缺少动态库或资源文件、只能打开却无法工作的应用。

## v1.0.7

- 批量出图页新增镜头多选工具栏，无需按 Ctrl 即可连续选择，并支持一键全选或清空。
- 新增“重新绘制已选镜头”：只重绘选中项，确认框会显示选中数量、已有图片数量、模型、分辨率和费用提示。
- 新图成功后才替换对应旧图；单项生成失败时继续保留原图片文件。

## v1.0.6

- 修复 Lite 与 Pro 同时无法出图的问题：Seedream 5.0 当前接口要求分辨率预设使用小写 `2k`、`3k`、`4k`，程序现会自动转换。
- 分辨率选项同步调整为当前接口支持的 2K、3K、4K；旧项目中的 1K、1.5K 自动迁移到 2K。
- 分辨率参数错误改为中文提示，避免直接显示难以理解的英文 400 响应。

## v1.0.5（2026-08-10）

- 修正 Seedream 5.0 高质量模型 ID 为官方的 `doubao-seedream-5-0-260128`，并自动迁移旧版错误 ID。
- 批量出图的模型下拉框改为始终可点击的 Lite / Pro 双按钮，点击后立即保存到当前项目。
- 当账号未在火山方舟开通所选模型时，将英文 404 转为明确的中文开通提示，并说明可以切换到已开通模型。
- 进一步锁定抖音参考视频的中近景表现：分镜分析与最终生图都以中近景、近景为主，人物优先胸像、半身或腰部以上，占画面高度约 55%～80%。

## v1.0.4（2026-08-10）

- 根据用户提供的抖音漫画推文样片，新增并默认启用“抖音漫画推文效果”；成片规则与图片画风完全分离，韩漫、日漫、国漫等都使用同一套剪辑表现。
- 每张图片固定轻微放大，只添加上下缓慢平移的两个位置关键帧；镜头按配音与 SRT 时间轴直接切换，不生成人物动作、镜头推拉或动态漫画。
- 剪映草稿与直接导出的静态漫视频统一使用醒目的黄色粗体、纯黑描边字幕，并放在画面下部安全区。
- 旧项目中的“上下交替关键帧”会自动迁移为新效果，既有角色、场景、分镜、图片和草稿记录不会丢失。

## v1.0.3（2026-08-10）

- 批量出图页新增 Seedream 5.0 Lite / Seedream 5.0 Pro 模型选择，选择结果按项目保存。
- 新项目与旧项目补全配置后默认使用 Lite 生成分镜图片，降低大量分镜出图成本；需要更高细节时可随时切换 Pro。
- 角色定妆和场景定景仍沿用连接设置中的默认模型，分镜模型选择不影响已固定的基础人物与场景资产。
- 接入官方 Lite 模型 ID `doubao-seedream-5-0-lite-260128`，继续支持人物、场景多参考图及现有分辨率与提示词优化选项。
- 每张完成的分镜会记录实际返回的模型，并在选中分镜的状态区显示，便于核对费用与质量。

## v1.0.2（2026-08-10）

- 新增角色换装关联：新角色可关联一个已有角色作为人物本体，角色列表会直接显示关联关系。
- 生成换装候选图时自动携带本体角色的已确认参考图，并硬性锁定脸型、五官、发型、发色、肤色、年龄、体态与身体比例，只允许修改服装、鞋袜和可穿戴配件。
- 本体角色改名时换装关联同步更新；删除本体时安全解除关联；人物与场景资产包会保留角色关联信息。
- AI 分镜与 Seedream 最终出图统一优先中近景、近景和中景；人物优先胸像、半身或腰部以上，除非剧情必须交代环境，否则避免远景、大全景、超远景和人物过小。

## v1.0.1（2026-08-10）

- 强化韩漫画风提示词：人物、场景、AI 分镜和最终镜头统一加入明确的二维韩系网络漫画表现要求。
- 增加禁止真人照片、写实摄影、影视剧照、真人皮肤纹理、3D 渲染和超写实风格的硬性约束，避免 Seedream 将韩漫人物生成成真人。
- 已有项目的角色和场景提示词会在打开、保存或生成时自动升级，无需删除原角色、场景或参考图。
- 更新韩漫画风预设，改用漫画化五官、线稿、赛璐璐与柔和渐变上色等更适合二维条漫的描述。

## v1.0（2026-08-10）

- 正式更名为“漫画推文”，主页采用多项目工作流；角色资产可跨项目复用，场景、分镜、素材和草稿按项目隔离。
- 完整支持角色定妆、场景定景、参考图确认与本地持久保存，并提供角色/场景及提示词的导入导出。
- 对接 Doubao Seedream 5.0 Pro，生成镜头时自动携带已确认的人物与场景参考图，并提供现代都市韩漫等画风。
- AI 可单独分析角色、场景或分镜，也可组合生成；长篇小说会分批发送并校验覆盖范围，避免镜头遗漏。
- 静态分镜采用一镜一行的扁平表格：提示词、角色和场景可直接编辑，支持免 Ctrl 多选、批量替换、人工合并与拆分镜头。
- 批量出图按分镜一一对应展示，支持进度反馈、图片放大以及变更角色或场景后的待重新生成标记。
- 可导入配音与 SRT 字幕并直接生成剪映本地草稿；每张静态图仅使用上下移动位置关键帧，画面时间与分镜文本精确对齐。
- 恢复并保留 AI 小说改文工作台，统一优化滚动区域、圆角控件、下拉选择与整体深蓝灰视觉风格。

## v0.10.9（2026-08-10）

- 重新生成缺失的 Windows 单文件程序。
- 静态分镜移除“页面主卡片 → 分镜卡片”的重复外层嵌套，分镜标题、批量替换、调整工具和镜头表格统一放在一个工作区中。
- 镜头行改为扁平表格样式，移除提示词、角色和场景内部重复的背景卡片与边框。
- 提示词、角色和场景仍保留各自滚动能力、直接编辑和即时保存；选中镜头时才显示强调边框。
- 收紧镜头行高度和间距，在不破坏七列对齐的前提下显示更多内容。

## v0.10.8（2026-08-10）

- 修复静态分镜行内场景下拉框在嵌套滚动区域中无法可靠展开、导致场景不能改选的问题。
- 场景栏改为常驻的单选列表，所有可用场景直接显示在镜头行中，普通点击即可切换并立即保存。
- 场景较多时列表支持独立滚动，并继续提供“无场景”选项。
- 保持表头与镜头行七列严格对齐，并验证真实选择事件能够正确改写对应镜头。

## v0.10.7（2026-08-10）

- 静态分镜每一行的角色栏改为可直接点击多选，无需进入其他编辑区，也不需要按 Ctrl。
- 每一行的场景栏改为可直接切换的下拉框，选择后立即保存到对应镜头。
- 行内修改角色或场景时，已生成图片会立即解除绑定并标记为待重新生成，避免继续误用旧画面。
- 表头与镜头行改用同一套七列网格定义，并按实际像素校验列起点和宽度，解决行列错位问题。

## v0.10.6（2026-08-10）

- 静态分镜改为真正的一镜一行编辑：每行直接显示并编辑该镜头的表情/动作提示词，不再使用列表下方的额外提示词框。
- 人物与场景批量替换固定到分镜列表上方，无需先点击片段即可选择“全部分镜”或“选中分镜”执行替换。
- 每行同步显示对应图片、原文、角色、场景和状态；提示词支持行内保存、失焦自动保存与 Ctrl+S，修改已出图镜头时会正确标记为待重新生成。
- 普通点击即可跨页多选镜头，继续支持全选、清空、合并相邻镜头和人工拆分单个镜头。
- 一百多个镜头自动按每页 20 个显示，分镜区保留独立滚动条和跨页选择状态，避免同时创建大量编辑框导致页面打开缓慢。

## v0.10.5（2026-08-10）

- 修复静态分镜提示词编辑区位于大尺寸分镜列表下方、首屏难以发现的问题。
- 缩短静态分镜列表的可视高度，让当前角色、场景和提示词编辑区紧接列表并进入首屏。
- 当前画面提示词改为绿色高亮卡片，明确标注“可直接修改”，并将“保存提示词”按钮移到卡片标题右侧。

## v0.10.4（2026-08-10）

- 静态分镜取消独立的右侧编辑框，角色、固定场景、表情动作提示词和批量替换全部移入“分镜与对应画面”工作区。
- 删除静态分镜页的整批重新拆分工具条，改为人工调整当前分镜结构。
- 新增“合并选中相邻镜头”：保留前后原文顺序并合并角色；场景不一致时留空等待人工确认，原图片标记为待重新生成。
- 新增“拆分当前镜头”：在原文中单击精确分界位置，用双色预览上下两个镜头，确认后只替换当前镜头且不会调用 AI。
- 合并或拆分后自动重新编号、保存项目并使旧剪映草稿失效，避免旧画面与新分镜结构错位。

## v0.10.3（2026-08-10）

- 静态分镜页参考批量出图页重新布局，左侧改为“对应图片、片段、角色、场景、状态”一镜一行的大尺寸列表。
- 已生成图片直接显示在所属分镜行，未生成分镜显示占位图；点击图片可放大，点击其他列可继续进行无 Ctrl 多选。
- 右侧保留当前分镜的角色多选、固定场景、表情动作提示词和批量替换，并收紧编辑框高度避免下方操作被挤走。
- 优化较窄窗口下的分镜列宽，确保状态列和右侧编辑区都能完整显示。

## v0.10.2（2026-08-10）

- 重构批量出图页，取消与分镜分离的混排图片画廊，改为每个分镜行固定展示自己的对应图片。
- 同一行同步显示分镜片段、出场角色、固定场景与生成状态；未出图时显示占位图，完成后只刷新对应行。
- 点击任一分镜行左侧图片即可放大查看，分镜列表保留独立滚动条和普通单击多选逻辑。
- 优化生成进度刷新，不再在每次进度变化时重建整张分镜列表。

## v0.10.1（2026-08-10）

- 场景描述与定景提示词明确跟随当前项目画风，AI 识别场景时会要求与角色定妆保持完全一致。
- 生成场景前统一补齐画风锁定、无人环境、固定空间布局和画幅约束，旧项目或手动填写的场景提示词同样生效。
- 场景定景页直接显示当前跟随的角色画风，切换项目画风后按新风格生成。

## v0.10.0（2026-08-10）

- 扩大剪映草稿页的 SRT 字幕素材卡，加入清晰的文件状态和全宽导入按钮，避免入口被压缩或遮挡。
- 角色定妆与场景定景改为直接展示候选图或已确认参考图，并可点击放大查看，无需逐个打开预览。
- 收紧角色、场景的描述和提示词编辑区高度，把页面空间留给图片与主要操作。
- 批量出图页新增已生成图片画廊，所有分镜图会直接显示；支持点击放大、滚轮缩放和拖动检查。
- 圆角卡片、输入框、按钮与下拉框改用高分辨率抗锯齿绘制，减轻四角的锯齿感。

## v0.9.9（2026-08-10）

- 原生纵向滚动条替换为无箭头、细轨道、圆角滑块的自绘样式，支持轨道点击、滑块拖动与悬停反馈。
- 卡片、工作区、镜头清单、镜头详情、文本编辑器、列表与表格统一增加圆角外壳和柔和边框。
- 下拉框重新设计圆角边框、内部留白、箭头、悬停状态和弹出列表配色。
- 输入框和操作按钮同步改为圆角控件，让各页面的边角语言保持一致。

## v0.9.8（2026-08-10）

- 所有长文本框、列表和表格统一加入可见的纵向滚动条，并让漫画制作的各步骤页面在小窗口中支持滚轮上下浏览。
- 分镜清单改为普通单击即可追加或取消多选，不再要求按住 Ctrl；新增“全选镜头”和“清空选择”。
- 重排静态分镜工作区：左侧专注镜头清单，右侧集中当前镜头编辑与批量替换，移除重复且拥挤的常驻进度侧栏。
- 全局视觉从粉色动漫主题调整为深蓝灰、暖白与青绿色的专业创作工具风格，统一侧栏、卡片、按钮、选中态和进度信息。

## v0.9.7（2026-08-10）

- 将静态分镜画面提示词简化为“对应人物 + 一个表情或动作”的短句；最终生图提示只补充参考图、画风、静态单幅和画幅信息。
- 静态分镜新增批量替换：可对全部分镜或多选分镜批量替换/移除角色，以及替换/清空固定场景。
- 修复角色、场景改名后镜头清单未及时更新的问题；名称编辑会自动保存并同步所有相关分镜。
- 修复关闭应用后角色定妆提示词可能丢失的问题，新增 450ms 自动保存和当前角色提示词导入/导出。
- “小说与项目”页面改为带滚轮的纵向滚动布局，小窗口无需放大也可操作底部按钮。
- 全局 UI 更换为粉色动漫主题，加入樱花、爱心、星光元素并统一卡片、按钮、选中态和进度颜色。

## v0.9.6（2026-08-10）

- 将 AI 分镜从动态漫导演逻辑改为静态漫逻辑：一个分镜只承载一个可被瞬间定格的主要动作状态和明确情绪。
- 对白换人、人物反应、站位或姿态变化、动作发生与结果、情绪及关键信息变化会优先拆成新的静态分镜，不再把连续过程压进一张图。
- 分镜提示词强制描述人物站位、定格姿态、手势、视线、表情、景别、机位、前后景和光线，并禁止动态运镜、多动作阶段、多时空、分格和连环画。
- AI 若仍返回推拉摇移、跟拍、环绕、变焦、转场等动态描述，程序会拒绝该批结果并自动重试。

## v0.9.5（2026-08-09）

- 修复静态漫图片切换位置与分镜剧情不对应的问题：不再按字幕条数平均切分，而是将每个分镜保存的原文与 SRT 内容逐字对齐。
- 分镜边界落在同一条字幕内部时，会按文字位置换算为对应音频时间；没有字幕时则按各分镜原文长度分配画面时长。
- 剪映草稿与可选 MP4 输出统一使用新的剧情对齐时间线。

## v0.9.4（2026-08-09）

- “生成静态漫”改为生成真正的剪映草稿，不再先把图片压成 MP4；每张分镜图片、配音和 SRT 字幕均保留为独立可编辑轨道。
- 每张图片写入两个可编辑的纵向位置关键帧，支持上下交替、统一向上、统一向下或无关键帧。
- 静态漫草稿页面新增独立进度条，按读取音频、写入图片、写入配音/字幕和保存草稿逐步显示百分比与当前动作。
- 新增剪映本地草稿目录设置，并支持草稿生成后自动启动剪映、重新打开已有草稿及打开草稿文件夹。
- 重新启用 AI 小说改文模块：支持导入/粘贴小说、自动拆章、当前章或批量改写、设定库、提示词预览和结果导出。

## v0.9.3（2026-08-09）

- 静态漫成片不再使用“动态漫画”概念：每个镜头始终使用原始静态分镜图，只添加向上或向下移动的起止关键帧。
- 移除推近、拉远和左右摇移模式，提供上下交替、统一向上、统一向下和完全静止四种选择。
- 新增“生成并打开剪映”和“打开剪映继续编辑”，成片完成后可直接启动剪映并定位 MP4 文件。
- Windows 会自动寻找本机已有的 FFmpeg、FFprobe 与剪映专业版，减少因工具路径为空导致的导出失败。
- 修正静态漫工作区、主页概览和设置页文案，明确视频合成是可选步骤，不会调用 AI 生成动画。

## v0.9.2（2026-08-09）

- 修复退出重进后角色与场景资产索引消失的问题：新版状态迁移到独立的 `ComicPostStudio` 数据目录，旧版程序无法再覆盖新项目数据。
- 启动时扫描本地 `_reference` 与 `_candidate` 图片，自动重建因状态文件损坏而丢失的角色和场景记录。
- 新增单实例锁，阻止同时打开两个漫画推文窗口互相覆盖状态。
- 每次保存前自动备份状态文件，最多保留最近 20 份，可用于后续恢复。

## v0.9.1（2026-08-09）

- 移除“每个片段字数”和“仅本地拆分”，分镜边界改由 AI 根据场景、动作、对白、情绪和时间变化自主判断。
- 本地只按技术上限把长文本拆成多个请求，AI 可在每个请求内返回任意合理数量的分镜。
- 新增 AI 拆分覆盖校验：所有分镜原文必须按顺序完整覆盖输入正文；漏字、改写、重叠或请求失败都会自动缩小批次重试，旧结果不会被残缺结果覆盖。
- 人物参考图提示词增加硬性隔离约束：仅生成单个角色和纯色无缝背景，禁止场景、建筑、家具、环境道具及其他人物；旧角色提示词在出图前也会自动补齐该约束。

## v0.9.0（2026-08-09）

- 产品更名为“漫画推文”，主页和侧栏移除视频混剪、小说改文等业务入口，聚焦漫画推文生产。
- 主页重构为多项目列表：制作新推文前先建立项目，可查看每个项目的场景、分镜、图片进度和最近保存时间。
- 小说、场景、分镜、图片、音频、字幕与成片按项目隔离；旧版单项目数据会自动迁移为第一个项目。
- 角色库升级为跨项目共享资产，角色定妆和本地参考图只保存一份，所有项目都可直接选择使用。
- 共享角色候选图和固定参考图统一存入公共本地角色目录，删除共享角色时会同步清理所有项目的角色绑定。
- 移除剪映与视频混剪打包依赖；仅保留漫画动态成片所需的 FFmpeg、FFprobe 与 MediaInfo。

## v0.8.0（2026-08-09）

- 固定人物和场景改为严格本地资产：只有本地参考图实际存在才算确认完成，远程临时 URL 不再单独满足一致性出图要求。
- 新增人物与场景 ZIP 资产包导出，完整携带名称、描述、提示词和已确认参考图。
- 新增跨电脑资产包导入，参考图会复制到当前电脑的项目素材目录并重建本地路径，同名资产自动合并。
- 资产包导入使用版本、清单、图片格式、大小和路径穿越校验，不会把文件写出项目素材目录，也不会覆盖已有本地文件。
- 删除角色或场景前新增明确的永久删除确认；只有用户确认后，才删除标准素材目录中的候选图和固定参考图。
- 角色和场景页面都增加“导入资产包”“导出资产包”入口，迁移后可直接继续进行分镜绑定和 Seedream 出图。

## v0.7.0（2026-08-09）

- 漫画工作台升级为六步生产线，在角色定妆与分镜编排之间新增独立“场景定景”步骤。
- AI 分析新增“只识别场景”和“角色 + 场景 + 分镜”，分批结果会建立稳定场景名称和可复用环境描述。
- 新增场景资产库，可生成无人物候选定景图、预览、确认保留或导入现有参考图，并长期保存到项目素材目录。
- 每个分镜可单选一个固定场景；重命名或删除场景时会同步更新所有分镜绑定。
- Seedream 出图同时提交已确认的角色参考图与场景参考图，并在提示词中锁定空间布局、建筑结构、家具、门窗、标志物、色彩和基础光线。
- 角色与场景参考图合计超过 Seedream 单次 10 张上限，或绑定场景尚未确认时，会在产生费用前明确阻止生成。

## v0.6.0（2026-08-09）

- AI 漫画分析新增“只识别角色”“只生成分镜”“角色 + 分镜”三种独立模式。
- 单独识别角色时保留现有分镜，单独生成分镜时保留角色库和已确认定妆。
- 小说先在本地按目标分镜长度切分并赋予稳定片段编号，再按约 3500 字分批发送给文本模型，避免单次上下文过长。
- 每批 AI 分镜必须逐一返回全部片段编号和完整原文；缺少、重复、异常编号或原文不完整会自动重试。
- 只有全部批次通过校验后才替换原有分镜；连续失败会保留旧结果并指出出错批次。

## v0.5.0（2026-08-09）

- 漫画工作台扩展为五步生产线，新增“音频成片”阶段。
- 角色定妆改为“生成候选 → 预览 → 确认保留”；只有确认后的图片才会作为后续镜头的人物参考图。
- 分镜编辑新增多选角色列表，一个镜头可包含并调整多个出场角色。
- 支持导入并归档配音音频和 SRT 字幕，按音频时长与字幕时间点分配每张分镜的展示时长。
- 每张静态分镜自动加入推近、拉远或左右平移动效，并通过 FFmpeg 输出带音频、烧录字幕的 H.264 MP4。

## v0.4.0（2026-08-09）

- 漫画生图服务从 Yunwu Midjourney 切换为火山方舟 Doubao Seedream 5.0 Pro，默认模型为 `doubao-seedream-5-0-pro-260628`。
- 角色定妆和分镜统一改用官方 `images/generations` 接口，支持 1K、1.5K、2K 与标准/极速提示词优化。
- 每个分镜会直接附带出场角色的本地定妆参考图，最多 10 张；生成结果立即归档到本地，增强人物一致性并避免临时 URL 过期。
- 新增现代都市韩漫画风预设，采用精致人物、修长比例、柔和渐变上色、戏剧化表情与竖屏网漫构图。
- 连接设置改为火山方舟 API 地址、模型 ID 和独立 ARK API Key，Key 继续使用系统安全凭据库保存。

## v0.3.2（2026-08-09）

- 进一步美化漫画工作台：新增深色品牌头图、项目成图计数和更醒目的主操作区。
- 步骤导航升级为带编号、说明和完成状态的双层卡片，当前步骤与已完成步骤更容易区分。
- 右侧项目进度改为独立状态卡，统一完成色、建议下一步、Yunwu 连接和当前任务进度。
- 优化输入框焦点、按钮悬停、角色列表与分镜表格，增加完成/失败状态和斑马纹显示。
- 调整分镜编辑器和小说正文编辑器的响应式尺寸，保证 1440×900 下所有操作完整可见。

## v0.3.1（2026-08-09）

- 参考 ArcReel、LumenX、LocalMiniDrama 等开源 AI 影视/漫画工作台，重做漫画推文的信息架构。
- 漫画制作改为“小说与项目 → 角色定妆 → 分镜编排 → 批量出图”四步工作流，当前页面只显示本阶段需要的操作。
- 新增顶部流程进度、右侧项目完整度和建议下一步，角色、分镜、成图数量一眼可见。
- 将 Yunwu API Key 与接口地址迁移到独立连接设置窗口，生成任务状态固定显示在右侧，不再与角色编辑混排。
- 分离角色资产库、分镜详情编辑器和出图生产队列，并保留原有项目、参考图、提示词与任务状态。

## v0.3.0（2026-08-09）

- 新增“漫画推文”工作台，可导入或粘贴小说，使用文本模型提取角色与分镜；未配置文本模型时也可按段落本地拆分。
- 新增角色定妆库，可生成或导入人物初始样貌，并把角色描述、任务 ID、远程地址和本地参考图保存到项目状态。
- 接入 Yunwu Midjourney Imagine、任务查询和 Action 选图接口，支持填写并安全记住独立的 Yunwu API Key。
- 分镜生成会自动关联角色参考图，以 `--cref`、`--cw`、固定人物描述和本地垫图共同增强人物一致性。
- 支持逐镜头生成、批量生成、失败项重试、任务进度记录、本地图片预览和素材目录归档。

## v0.2.6（2026-08-10）

- 将导入视频素材的固定画面速度由 1.5 倍调整为 1.75 倍，FFmpeg 成片和剪映草稿同步生效。
- 主音频、背景音乐与字幕继续保持原速，最终时间线仍严格匹配主音频时长。
- 素材原声继续保持静音。

## v0.2.5（2026-08-09）

- 导入的视频素材画面统一按 1.5 倍速播放，FFmpeg 成片和剪映草稿保持一致。
- 主音频、背景音乐和字幕时间轴保持原速；有主音频时，画面会按加速后的可用时长循环或截断，最终长度仍与主音频精确一致。
- 素材自带音频继续保持静音，不会混入配音或背景音乐。

## v0.2.4（2026-08-08）

- 导入的视频素材全部默认静音，素材文件中的人声、环境声和音乐不会进入剪映草稿。
- 主音频和背景音乐继续使用独立音轨，不受素材静音影响。
- MP4 导出与剪映草稿现在保持一致：只输出用户主动导入的主音频和背景音乐。

## v0.2.3（2026-08-08）

- 重做视频素材时间线逻辑，删除“读取失败默认 5 秒”和“最多取 8 秒”的错误限制。
- 导入时读取并保存原片真实时长，旧版导入的 5/8 秒素材会自动升级为完整可用时长。
- 新增“均衡混剪”模式：根据主音频时长在所有素材之间公平分配画面，避免只用第一段素材。
- 新增“顺序完整播放”模式，可优先播完前一段素材后再接下一段。
- 取用范围超过原片结尾时自动修正，并可一键恢复所选素材的完整时长。
- FFmpeg 成片与剪映草稿共用同一套时间线分配规则，末段仅按目标音频精确截断。

## v0.2.2（2026-08-07）

- 新增 API Key 安全记忆：Windows 使用系统凭据管理器，macOS 使用系统钥匙串。
- 再次打开应用或切换服务商时，会自动回填对应服务商的 API Key。
- 新增“安全记住 API Key”开关和“清除已保存 Key”按钮。
- API Key 仍不会写入 `state.json`、项目源码或 GitHub 仓库。

## v0.2.1（2026-08-06）

- 修复 Kimi 测试连接时报 `invalid temperature` 的问题。
- Kimi 请求不再显式发送温度参数，由模型接口自动采用合法默认值。
- DeepSeek、千问、智谱 GLM 和自定义接口继续使用创作任务指定的温度参数。

## v0.2.0（2026-08-06）

- 新增 DeepSeek 官方接口预设，默认模型为 `deepseek-v4-flash`。
- 新增千问（阿里云百炼）接口预设，默认模型为 `qwen-plus`。
- 新增智谱 GLM 接口预设，默认模型为 `glm-5.2`。
- 新增 Kimi 接口预设，默认模型为 `kimi-k3`。
- 新增服务商切换、分服务商内存 Key、环境变量读取与“测试连接”功能。
- 继续支持 OpenAI 和自定义 OpenAI Chat Completions 兼容接口。
- Windows 与 macOS 构建产物改用版本号命名，避免覆盖旧版本。

## v0.1.0

- 首个桌面版：视频混剪、音频与字幕导入、剪映草稿生成、小说改文。
